<%= simple_form_for pet do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :species, as: :select, collection: Pet::SPECIES %>
  <%= f.input :found_on, as: :date %>
  <%= f.submit 'Submit', class: 'btn btn-primary d-block mx-auto' %>
<% end %>
